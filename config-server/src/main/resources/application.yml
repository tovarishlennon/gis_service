server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tovarishlennon/configurations.git
          search-paths: config
      label: main
      allow-override: true
